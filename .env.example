VITE_SUPABASE_URL=https://vgakksutqrupmbffjedk.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
# Get anon key: Supabase Dashboard → Project Settings → API → anon public
# For phone SMS OTP, also configure: Supabase Dashboard → Authentication → Providers → Phone → Enable + Twilio credentials

# --- Server-only (Vercel env, NEVER prefix with VITE_) ---
# POST /api/analyze-batch needs both or it returns provider_not_configured → review_required
# ROBOFLOW_API_KEY=your_roboflow_key_here
# OPENROUTER_API_KEY=your_openrouter_key_here
# Optional overrides:
# OPENROUTER_MODEL_ID=qwen/qwen2.5-vl-72b-instruct
# GRADING_A_MAX=5
# GRADING_B_MAX=10
# GRADING_C_MAX=20
